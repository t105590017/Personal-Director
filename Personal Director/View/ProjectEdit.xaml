<Page
    x:Class="Personal_Director.ProjectEdit"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Personal_Director"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.TopAppBar>
        <CommandBar>
            <CommandBar.Content>
                <Button x:Name="PrePage" Style="{StaticResource NavigationBackButtonNormalStyle}" VerticalAlignment="Top" Click="PrePage_Click"/>
            </CommandBar.Content>

            <!--<AppBarButton Icon="Delete" Label="Delete"/>
            <AppBarButton Icon="Save" Label="Save"/>-->
        </CommandBar>
    </Page.TopAppBar>

    <Grid>
        <TextBox x:Name="textBox" HorizontalAlignment="Center" Text="New Project" TextWrapping="Wrap" VerticalAlignment="Center" Height="316" Width="608" FontSize="100" VerticalContentAlignment="Center"/>
    </Grid>

</Page>
